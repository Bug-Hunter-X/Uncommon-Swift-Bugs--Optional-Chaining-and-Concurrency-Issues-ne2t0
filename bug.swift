let numbers = [1, 2, 3, 4, 5]

let doubledNumbers = numbers.map { $0 * 2 }

print(doubledNumbers) // Output: [2, 4, 6, 8, 10]

//This is a simple example but it doesn't show any uncommon error or bug.
//Uncommon errors in swift can be related to memory management, concurrency, or compiler optimizations.  These can be hard to track down and are often subtle.